package com.clafu.controller;

import org.slim3.controller.Controller;
import org.slim3.controller.Navigation;

public class UserImageController extends Controller {

    @Override
    public Navigation run() throws Exception {

//        String userId = asString("id");
//
//        UserModel userModel = UserService.getOrNull(Long.valueOf(userId));
//
//        if(userModel == null || userModel.getProfileImageBlob() == null) return null;
//
//        byte[] imageData = userModel.getProfileImageBlob().getBytes();
//
//        OutputStream out = response.getOutputStream();
//        out.write(imageData);
//        out.flush();

        return null;
    }
}
